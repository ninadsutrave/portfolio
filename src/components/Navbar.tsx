import React, { FC, ReactElement } from 'react';

const Navbar: FC = (): ReactElement => {
    return <div className="sticky flex justify-end top-1/2 right-5 z-10">
        Navbar
    </div>
}

export default Navbar